# Data Generation Dependencies
# For project-wide management, see ../pyproject.toml (use with uv)

shapely>=2.0.2
numpy>=1.24.3
pandas>=2.0.3
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# To install with uv (recommended):
#   uv pip install -e .
#
# To install with pip:
#   pip install -r requirements.txt

